# ============================================================
# Business Churn Prediction — dependencies (full project)
# Install:  pip install -r requirements.txt
# ============================================================

# ---- Core data / numeric ----
pandas>=2.2
numpy>=1.26
pyyaml>=6.0
python-dotenv>=1.0

# ---- Visualization ----
matplotlib>=3.8
seaborn>=0.13

# ---- Statistics & probability (Phase 3) ----
scipy>=1.12
statsmodels>=0.14
lifelines>=0.28          # survival analysis (Kaplan-Meier, Cox)

# ---- Machine learning (Phase 4) ----
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
imbalanced-learn>=0.12   # SMOTE / class imbalance
optuna>=3.6              # hyperparameter tuning

# ---- Experiment tracking & explainability (Phase 4-5) ----
mlflow>=2.12
shap>=0.45

# ---- Big data (Phase 1-2) ----
pyspark>=3.5

# ---- GenAI layer (Phase 5) ----
anthropic>=0.25

# ---- Serving & app (Phase 6) ----
fastapi>=0.110
uvicorn>=0.29
streamlit>=1.34

# ---- Monitoring (Phase 6) ----
evidently>=0.4

# ---- Dev tooling ----
jupyterlab>=4.1
pytest>=8.1
pre-commit>=3.7
ruff>=0.4
black>=24.0
