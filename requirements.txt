# ============================================================
# RUNTIME dependencies — what the deployed app needs.
# Streamlit Community Cloud and the Docker image install THIS file.
# Full dev/analysis stack: requirements-dev.txt
# ============================================================

pandas~=2.3
numpy~=2.4
pyarrow>=15
pyyaml>=6.0
python-dotenv>=1.0
pandera~=0.32
joblib>=1.5

# Pinned to the versions that trained the committed model artifact —
# unpickling across sklearn versions is unsafe.
scikit-learn==1.9.0
lightgbm==4.6.0

shap~=0.52
streamlit~=1.58
fastapi>=0.110
uvicorn>=0.29
google-genai>=1.0
